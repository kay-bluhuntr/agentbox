# Local docker-compose dev secrets. Copy to .env (gitignored) before `make dev`,
# or let `make dev` copy it for you. These are throwaway local-only credentials;
# real deployments inject AGENTBOX_DATABASE_URL from a Kubernetes Secret.
POSTGRES_PASSWORD=agentbox
AGENTBOX_GPU_ENABLED=false